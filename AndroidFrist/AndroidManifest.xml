<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidfrist"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

     <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
     <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /><!-- 授予sd卡写入操作 -->
     <uses-permission android:name="android.permission.INTERNET"/> <!-- 授予上网权限 -->
     <uses-permission android:name="android.permission.WAKE_LOCK"/>
     <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
     <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- 开启录音 -->
     <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 读取文件 -->
	

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:minSdkVersion="11"
        android:theme="@style/AppTheme" >
        
        <!-- WelcomeActity -->
        <activity
            android:name="com.huir.android.chat.ChatActivity" 
            android:windowSoftInputMode="adjustResize|stateHidden"
 	 		android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name="com.huir.android.activity.MainActivity"></activity>
        
        <activity android:name="com.huir.android.activity.RegActivity"></activity>
        
        <!-- <activity android:name="com.huir.android.activity.ChatActivity"></activity> -->
       <!--  <activity android:name="com.huir.android.activity.tab.TabActivity"></activity> -->
        <service android:name="com.huir.android.net.NetService"></service>
    </application>

</manifest>
